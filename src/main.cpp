#include <iostream>
#include <sstream>

#include "Mutator.h"
#include "Display.h"

int main()
{
    Mutator mutate;
    mutate.poolMutator(100);
    //Display disp;
    //disp.run();

    return EXIT_SUCCESS;
}
